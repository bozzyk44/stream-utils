# For local development of stream-utils itself (running tests against real APIs,
# smoke-checking helpers). Consumer projects do NOT use this file — they manage
# their own .env separately.

TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
PROXYAPI_KEY=
